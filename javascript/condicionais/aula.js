// operadores booleanos: true false
let verdadeiro = true;
let falso = false

const numero = 0;

// % é resto da divisão
let isPar = numero % 2;


// if padrão
// === faz uma comparação até dos tipos
if (numero === 0)
    console.log('O número é inválido');
else if (ePar === 0)
    console.log('O número é par');
else
    console.log('O número é ímpar');
